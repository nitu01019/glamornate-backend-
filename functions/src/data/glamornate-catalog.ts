/**
 * Canonical catalog lives in `@glamornate/data-catalog`
 * (see `packages/data-catalog/`). This re-export preserves existing
 * backend import sites (`./glamornate-catalog`) during the transition.
 */

export * from '../shared/catalog';
